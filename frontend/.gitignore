*.env*
!.env.example
node_modules/
.next/
dist/
.vercel/
coverage/
*.log
.DS_Store
tsconfig.tsbuildinfo
/test-build/

# Playwright UI audit: minted QA JWT + run artifacts, never commit.
# Only the secret and the run output — the specs themselves are tracked.
/e2e/.auth.json
/test-results/
/playwright-report/

# MediaPipe runtime assets (~29MB of wasm + .task model bundles). Reproducible from
# scripts/fetch-mediapipe-assets.mjs, which runs on postinstall — never commit them.
/public/mediapipe/
test-build
tsconfig.test.json

